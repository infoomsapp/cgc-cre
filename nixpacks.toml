[phases.setup]
nixPkgs = ["python312", "gcc", "libpq"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "sh -c 'uvicorn app.main:app --host 0.0.0.0 --port ${PORT} --workers 2 --loop uvloop --http httptools'"
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "cgc_core"
version = "0.1.0"
description = "CGC Core Engine"
authors = [{name="Olympus Mont Systems LLC"}]
dependencies = []